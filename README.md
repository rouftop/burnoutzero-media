# BurnoutZero social media

Public hosting for BurnoutZero social posts, so Buffer can fetch the media by URL.

- `videos/` — vertical 1080x1920 videos with sound
- `slides/SlideshowNN_*/` — five 1080x1920 slides per carousel
